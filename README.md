# nit.content

Frontmatter migration tooling and review workflow: [migration/README.md](migration/README.md).

The proposed bundled content layout lives in [`content-repo/`](content-repo/). The legacy `content/` and `pipeline/` trees remain unchanged during review.
